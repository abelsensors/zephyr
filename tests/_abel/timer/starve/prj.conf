CONFIG_ZTEST=y

#added from apps prj.conf:

# rtc timer driver, add to board config if finished
CONFIG_SYS_CLOCK_HW_CYCLES_PER_SEC=16000000

CONFIG_COUNTER_RTC_STM32=n

CONFIG_STM32_RTC_TIMER=y
CONFIG_STM32_RTC_TIMER_LSE=y

#if want power management, the rtc should be used as kernel system clock
CONFIG_PM=y